language: java
install: gradlew assemble javadoc
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  target_branch: javadoc
  local_dir: docs
  on:
    branch: master
